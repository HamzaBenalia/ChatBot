version: "3.1"

rules:
  - rule: Handle greeting
    steps:
      - intent: greet
      - action: utter_greet

  - rule: Default fallback action
    steps:
      - intent: nlu_fallback
      - action: action_default_fallback
